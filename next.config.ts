import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    images: {
    domains: ['raw.githubusercontent.com', 'penguinui.s3.amazonaws.com', 'images.unsplash.com'],
  },
};

export default nextConfig;
